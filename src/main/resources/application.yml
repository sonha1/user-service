spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/gtel-micro
    username: root
    password: 123123
    hikari:
      pool-name: HikariPool
      minimum-idle: 5
      maximum-pool-size: 10
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
      leak-detection-threshold: 0
jwt:
  prefix: Bearer
  secret: acMbf@2024
  refreshSecret: reMbf@2024
  validity: 86400

server:
  servlet:
    context-path: /user-service/api